# @vumo/preview

## 0.2.0

### Minor Changes

- c1d03fe: Initial public release. Includes the Vue primitives (`useCurrentFrame`, `useVideoConfig`, `defineComposition`, `Sequence`, `Audio`, `delayRender`/`continueRender`), the scrubber preview, a Puppeteer + FFmpeg render pipeline with parallel worker pool, deterministic per-frame re-seeding, and the `vumo render` CLI.

### Patch Changes

- Updated dependencies [c1d03fe]
  - @vumo/core@0.2.0
